export const base = 0;
export const above = 1;

export const localeFlag = above + base;
export const headerOverlay = above + base;
export const headerMask = above + headerOverlay;
export const headerContainer = above + headerOverlay;
export const navigation = above + headerContainer;
